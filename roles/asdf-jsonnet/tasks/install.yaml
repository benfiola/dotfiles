- name: install | install asdf plugin + version
  become: true
  become_user: "{{user_name}}"
  asdf:
    plugin: "{{asdf_jsonnet_plugin}}"
    version: "{{asdf_jsonnet_version}}"
    state: present
    executable: "{{asdf_executable}}"
    global_: true
