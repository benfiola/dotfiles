---
- tags: [graphical]
  block:
    - when: not (linux or debian)
      block:
        - name: unimplemented
          empty: {}

    - when: darwin
      block:
        - name: brew install coreutils
          community.general.homebrew:
            name:
              - coreutils
            path: "{{brew_path}}"
    
    - when: linux or darwin
      block:
        - name: link zshrc file
          ansible.builtin.file:
            src: "{{dotfiles_path}}/roles/lscolors/files/zshrc.sh"
            path: "{{user_profiled_path}}/00-lscolors.sh"
            state: link
...