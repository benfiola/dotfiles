# ulauncher

## Usage

```
ln -s <directory>/theme ~/.config/ulauncher/user-themes/theme
ln -s <directory>/settings.json ~/.config/ulauncher/settings.json
cat <directory>/keybindings.gsettings | xargs -l -t gsettings set
```
